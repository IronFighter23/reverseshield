{
  "name": "@reverseshield/reporting-api",
  "version": "0.0.0",
  "private": true,
  "description": "ReverseShield reporting API — ingests events, serves dashboard queries.",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "lint": "echo 'reporting-api lint: no-op in Phase 0' && exit 0",
    "test": "echo 'reporting-api test: no-op in Phase 0' && exit 0",
    "build": "echo 'reporting-api build: no-op in Phase 0' && exit 0",
    "start": "echo 'reporting-api start: no-op in Phase 0' && exit 0"
  }
}
