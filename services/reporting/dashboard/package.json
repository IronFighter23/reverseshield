{
  "name": "@reverseshield/reporting-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "ReverseShield reporting dashboard — React + Vite + Tailwind (per SPEC §6).",
  "type": "module",
  "scripts": {
    "lint": "echo 'reporting-dashboard lint: no-op in Phase 0' && exit 0",
    "test": "echo 'reporting-dashboard test: no-op in Phase 0' && exit 0",
    "build": "echo 'reporting-dashboard build: no-op in Phase 0' && exit 0",
    "dev": "echo 'reporting-dashboard dev: no-op in Phase 0' && exit 0"
  }
}
