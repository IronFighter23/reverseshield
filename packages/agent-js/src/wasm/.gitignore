# Entire directory is generated by scripts/prepare-wasm.mjs.
# Real wasm-bindgen output (or stubs) are written here on every `npm run wasm`,
# `npm test`, or `npm run build`. Nothing here belongs in the repo.
*
!.gitignore
